# had a problem with tensroflow
# it seems like it requires anaconda to be installed
# add the code for embedding here
class EmbeddingsGenerator:
    def generateFromUrl(self, imageUrl):
        #download the image
        #calculate the embedding of the image and return it
    def generateFromImage(self, image):